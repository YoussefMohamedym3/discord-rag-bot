IS_PERSISTENT=TRUE                  # Forces the DB to save to disk; otherwise, data is lost on restart.
ANONYMIZED_TELEMETRY=FALSE          # Stops the container from sending usage data to the cloud (Privacy/Speed).
PERSIST_DIRECTORY=/chroma/chroma    # Internal folder where data is written (Must match the path in docker-compose volumes).